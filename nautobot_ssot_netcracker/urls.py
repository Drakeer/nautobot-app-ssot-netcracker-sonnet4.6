"""URL configuration for the NetCracker SSoT app.

Currently empty — all sync triggering is done through the Nautobot Jobs UI
and the nautobot-app-ssot dashboard. Add custom views here if needed in future.
"""

from django.urls import path

urlpatterns = []
